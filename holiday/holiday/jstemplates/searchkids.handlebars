{{#each kids}}<a class="list-group-item accordion-toggle" data-toggle="collapse" href="#collapse{{id}}" data-parent="#accordion"><h3 class="list-group-item-heading">{{desc}}{{#if family}}, in family {{family}}{{/if}}{{#if urgent_need}}<span class="glyphicon glyphicon-exclamation-sign pull-right"></span><p class="pull-right" style="padding:0px 10px 0px 0px"><em>in urgent need</em></p>{{/if}}</h3></a><div id="collapse{{id}}" class="panel-collapse collapse"><div class="panel-body"><div class="list-group"><p class="list-group-item-text lead">Program: <a target="_blank" data-original-title="{{program}} website" rel="tooltip" href="{{program_site}}">{{program}}</a><a href="javascript:void(0)" onclick="confirm_sponsor('{{id}}', '{{name}}')" class="btn btn-success sponsor-kid pull-right" data-original-title="sponsor {{name}}" rel="tooltip">Sponsor <span class="glyphicon glyphicon-ok"></span></a><br />Clothing size: {{clothing_size}}<br /></p><div class="list-group-item collapse"><div class="btn-toolbar lead"><strong>Wish list</strong></div></div>{{#with wishlist}}{{#each this}}<div class="list-group-item"><h4 class="list-group-item-heading">{{wish}}</h4>{{#if notes}}<p class="list-group-item-text">{{notes}}</p>{{/if}}</div>{{/each}}{{/with}}</div></div></div>{{/each}}
